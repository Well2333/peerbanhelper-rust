---
date: 2026-06-19
commit: 305ba0b
type: test
scope: 真机验证（M2 get_peers + M3 自动封禁）
guideline_changed: false
---

# 真机 get_peers + 规则自动封禁验证

## 做了什么
- 用户在 your-qb.example.com 挂 4 个活动种子（正常 qB/µTorrent peer）。
- 临时配置 `peer-id-blacklist: STARTS_WITH "-UT"` 运行 release 成品 ~12s：
  - ✅ get_peers 解析真实 peer（client/peer_id/ip:port）。
  - ✅ 规则命中并自动封禁 6 个 µTorrent peer（日志逐条）。
  - ✅ curl 旁证 qB `banned_IPs` 恰为这 6 个 IP（字节级）。
  - 测试后恢复 qB `banned_IPs` 至原值。
- 默认规则对在场正常客户端不命中（符合预期=不误封）。

## 为什么
- 兑现待测报告：真机验证拉 peer + 规则触发的自动封禁全链路（M2 解析 + M3 引擎）。

## 影响的文件 / 范围
- 仅 test-status；无代码改动。

## 注意事项 / 后续
- M2+M3 核心链路（登录→拉 torrents→拉 peers→规则→自动封禁→写 qB→历史）真机全程通过。
- 仍待：增量封禁(`increment-ban=true`)、EE shadowban(需服务端开启)。
