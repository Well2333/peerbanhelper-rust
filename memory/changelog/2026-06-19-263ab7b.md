---
date: 2026-06-19
commit: 263ab7b
type: test
scope: 真机验证（M2 下载器）
guideline_changed: false
---

# 真实 qBittorrent 联网验证 M2 登录 + 封禁写入

## 做了什么
- 用户提供联网实例 `https://your-qb.example.com:50443`（qB v5.1.3.10，Enhanced Edition;libtorrent 1.2.20）。
- 用 release 成品 + curl 旁证，验证 M2 下载器对真实 qB：
  - ✅ 登录（admin/密码 → `/auth/login` "Ok." + cookie SID）。
  - ✅ 版本/buildInfo 拉取与解析（5.1.3.10→5.1.3）。
  - ✅ `get_torrents`（active，0 个，无错）。
  - ✅ `apply_ban_list` 全量：Web 手动封 `198.51.100.7` → wave 下发 → qB `banned_IPs` 字节级变更确认。
  - ✅ Web API 手动封禁链路。
- 测试后恢复 qB `banned_IPs` 至原值 `198.51.100.10`;`enable_multi_connections_from_same_ip` 全程未变。

## 为什么
- 兑现待测报告 M2 的真机验证项;确认面向真实下载器的关键路径（登录+封禁写入）正确。

## 影响的文件 / 范围
- 仅 `memory/test-status/{已测记录,待测报告}.md`。无代码改动（验证现有 release 成品）。

## 注意事项 / 后续
- **测试期间** qB `banned_IPs` 曾被 pbh 短暂接管（首轮清空→封测试 IP），测试结束已恢复原值——pbh 设计上对 banned_IPs 权威管理。
- 该 qB 无活动种子，故 `get_peers`/规则自动封禁/增量封禁/EE shadowban **仍待**有 peer 时验证。
