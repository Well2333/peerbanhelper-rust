---
date: 2026-06-19
commit: 6b1efde
type: feature
scope: repo (docs + workspace 骨架)
guideline_changed: false
---

# 预研报告、施工指南与 Rust workspace 骨架

## 做了什么
- 完成 PeerBanHelper(Java→Rust) 重写的预研与规划，产出 4 份文档：
  - `docs/01-research-report.md`：系统总览、8 大子系统逐项分析、技术选型、风险登记册
  - `docs/02-construction-guide.md`：M0–M10 里程碑、任务拆解、验收标准、对拍策略
  - `docs/03-api-contract.md`：前端复用基准（全部 REST/WS 端点，标注已删的 PBH Plus）
  - `docs/04-db-schema.md`：嵌入式 SQLite 14 张表与关键分析 SQL
- 初始化 12-crate Cargo workspace 骨架（`pbh-domain`..`pbh-server`），骨架阶段 std-only。
- 落地并单测可隔离纯逻辑（18 测全绿）：PeerAction 优先级合并、规则匹配引擎 False-短路优先级、
  i18n `{}` 顺序占位填充、下载器工厂分发、StdResp 信封、恒等 NAT 注入。
- 二进制 `pbh` 可运行（打印启动横幅）。

## 为什么
- 用户要求：把 PBH 重写为 Rust、零额外部署依赖（嵌入式 SQLite）、保留全部封禁在线功能、
  复用前端；先交付报告+施工指南+项目骨架。
- 骨架 std-only 是为满足守则第 7/8 条：在离线纯净宿主可编译可测，最大化可测面。

## 影响的文件 / 范围
- 新增 `docs/01-04`、`Cargo.toml`、`crates/**`、`Cargo.lock`。
- 不触及既有运行系统（绿地新建）。

## 注意事项 / 后续
- 三项已确认决策见 `memory/guidelines/01`：AutoSTUN 留接口、PBH Plus 整体删除(含13端点)、
  qB+qBEE only。
- 外部依赖（serde/tokio/axum/sqlx/reqwest 等）尚未引用，未联网验证版本兼容性——见
  `memory/test-status/待测报告.md`；M0 首次引入时需联网验证（rustc 1.75 或升级 toolchain）。
- clippy 当前环境未安装，未运行。
- 下一步：按 `docs/02` 从 M0（地基：配置加载 + SQLite 层 + 日志 + AppContext）开始。
