---
date: 2026-06-19
commit: ee7c2ce
type: guideline
scope: memory/guidelines
guideline_changed: true
---

# 同步 v2 到架构/编码规范（去前端 API 对拍与 i18n）

## 做了什么
- `02-architecture.md`：等价性边界去掉「关键 API JSON」,补注 v2 弃用 Vue 前端 → 自研 API/UI 不对拍;
  i18n(TranslationComponent)作废,后端单语言纯字符串。
- `03-coding-conventions.md`：序列化对齐改为「仅 BTN/下载器须与上游 Gson 一致」;自有 API 用
  `ApiResp { ok, data, error }`/分页 `{page,size,total,items}`,不与上游兼容;删 TranslationComponent 提及;
  对拍清单第 4 项由「API 响应 JSON」改为「BTN 上下行报文」。

## 为什么
- bf6c6bf 转向 v2 时,两份一级规范仍残留 v1 的前端 API 字节级对拍与 i18n 约定,与新方案矛盾。
  守则第 1 条要求规范改动即时同步——本提交补齐(bf6c6bf 的 changelog 已预告此项)。

## 影响的文件 / 范围
- 仅 `memory/guidelines/02`、`03`。无代码改动。

## 注意事项 / 后续
- 至此 guidelines 全量与 docs/05(v2) 一致。

## 规范同步（guideline_changed = true）
- 更新 `02-architecture`、`03-coding-conventions`：移除前端 API 对拍与 i18n 要求,明确自有 API 自定义信封。
