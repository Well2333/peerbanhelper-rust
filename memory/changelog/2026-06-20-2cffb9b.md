---
date: 2026-06-20
commit: 2cffb9b
type: feat
scope: GeoIP 回填 + 测试补全
guideline_changed: false
---

# feat: GeoIP peer_geoip 回填 + 补全测试覆盖

## 做了什么
- **peer_geoip 回填**：封禁历史写入时,若 GeoIP 可用则查询并把 `IpGeoData` 序列化为 JSON 存入 `history.peer_geoip`。`NewBanHistory` 加 `peer_geoip` 字段 + insert 列;`BanManager` 加 `geoip` 注入(main 传入)。
- **封禁列表富化**：`list_bans` 用 `WebState.geoip` 按需查询每个封禁 IP 的地理(国家/城市/ASN),填 `geo` 字段 → WebUI 封禁列表的「地理信息」从占位变为实值(GeoIP 可用时)。
- **测试补全**：
  - `pbh-web` envelope 4 测试(此前 0):ApiResp ok/empty/err + Page 序列化形状。
  - `pbh-geoip` 2 测试:无 mmdb 文件 → `load` 返回 None(降级);IpGeoData 序列化。

## 验证
- clippy/fmt 干净;`cargo test --workspace` **94 单测全绿**(+6:web 4 / geoip 2)。
- 真机:无 mmdb → 全程降级(`geo=null`),服务健康(modules 5/无 panic),封禁列表含 geo 字段。

## 注意
- 本环境无 MaxMind mmdb,peer_geoip / geo 恒 null;放 `<data>/geoip/*.mmdb` 即生效。
- WebUI 封禁列表 `b.geo` 已能显示富化的地理串(GeoIP 可用时)。
