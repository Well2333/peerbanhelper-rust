---
date: 2026-06-20
commit: 761f87a
type: feat
scope: BTN 上行补全(swarm + heartbeat)
guideline_changed: false
---

# feat: BTN 上行补全 —— SwarmTracking + submit_swarm + heartbeat

## 做了什么
- **`pbh-storage/swarm.rs`**：`tracked_swarm` 批量 upsert(单事务)——带 offset 的单调累加处理 peer 重连归零(新报告值 < 旧 → 旧值累进 offset,真实累计 = offset+当前);`query_btn_swarm`(游标 `last_time_seen,id` 分页);`clear_tracked_swarm`(启动清空临时表)。
- **`BanManager`**：每轮 wave 把**全部 peer**(不止被封的)记入 `tracked_swarm`;`track_swarm` 开关(= `btn.enabled && btn.submit`)+ `swarm_row` 助手。`main` 启动时清空临时表。
- **`pbh-btn`**：`BtnSwarm` 序列化模型(时间 **ISO 8601**,`to/from_peer_traffic` + `_offset`,speed/speed_max);`client.heartbeat`(`{"ifaddr":"default"}`→外网IP);调度新增 `submit_swarm`(游标 gzip)+ `heartbeat` arm;`iso8601` 助手(chrono)。
- submit_histories:该 BTN 服务端 config **未 offer**(只 offer submit_swarm),故不实现。

## 验证
- clippy/fmt 干净;`cargo test --workspace` **88 单测全绿**(新增 swarm upsert offset / clear+cursor 2 个)。
- **真机端到端**(真实 BTN sparkle.ghostchu.com,凭证+submit=true)：
  - swarm 跟踪:`tracked_swarm` 实时写入(观测到 11 条)。
  - 上行:`BTN 已上报 6 条封禁` + **`BTN 已上报 10 条 swarm (游标→…,131)`**。
  - **`BTN 心跳:外网 IP 203.0.113.10`**。
  - 下行 rules/denylist(230KB)/allowlist 同时正常。**BTN 下行+上行全部 ability 闭环跑通**。

## 注意
- swarm 时间用 ISO 8601(对应上游 OffsetDateTime),与 submit_bans 的 epoch millis(Timestamp)不同 —— 已分别处理。
- BTN config 拉取偶发网络瞬断(600s 重试);重试后恢复。
- 至此 BTN 全部服务端 offer 的 ability 均实现并真机验证。
