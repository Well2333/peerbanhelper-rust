---
date: 2026-06-20
commit: 95c6988
type: feat
scope: M9 收尾（阶段 F）
guideline_changed: false
---

# M9 收尾：WebUI 订阅状态面板 + 文档收口

## 做了什么
- **WebUI**：规则配置页新增「IP 黑名单订阅」状态表（rule_id / 名称 / 条数 / 最后更新 / 启用状态），读 `/api/sub/rules`，进入规则页自动加载。订阅的增删改仍经上方 YAML 编辑器（`module.ip-address-blocker-rules`），此表为只读状态展示。
- **文档收口**：
  - `STATUS.md` 重写为「M0–M8 全部里程碑代码完成」,列已交付/已验证/剩余打磨。
  - `roadmap.md`：M4/M5/M6/M8 里程碑标记 ✅ 并补完成说明与留待项。
  - `test-status/已测记录.md`：追加 A–F 六阶段实现 + 真机验证汇总。
- `dist/pbh-0.1.0-linux-x86_64.tar.gz`（5.7M）已重新打包。

## 为什么
- 把 M6 订阅能力接入 UI（用户能看到订阅加载状态）;把本轮（A–F 六阶段）的成果固化进 handoff 文档,便于后续接续。

## 验证
- `cargo test --workspace` **85 单测全绿**;clippy/fmt 干净。
- 真机综合（release 实跑 well404-qb）：在线、4 模块、PCB 载入 29 条续算、112 peer/5 wave、新 UI 28KB、`GET /` 200。

## 本轮（2026-06-20，A–F 六阶段）总览
1. **A** WebUI 多页改版 + 运行统计 + profile 热重载（`54be292`）。
2. **B** M4 余下 4 规则（`4e554c1`）。
3. **C** M5 PCB 核心 + 持久化（`c585194`/`4fad1ff`）。
4. **D** M6 IP 订阅 + IPBlackList + GeoIP（`151a035`/`2b6fde9`）。
5. **E** M8 BTN 云端（`553be67`）。
6. **F** M9 收尾（本提交 `95c6988`）。

→ **M0–M8 全部里程碑代码完成**。剩余为需外部条件的打磨（BTN 真机对拍需账号、GeoIP 需 mmdb、completed_size、WS 日志流等），见 STATUS.md 待办。
