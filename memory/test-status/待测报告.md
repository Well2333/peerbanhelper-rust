# 待测报告

> 只能人工 / 特定环境验证的项。骨架阶段大多依赖尚未引入，故多为「实现该里程碑时验证」。

## 工具链

- [ ] `cargo clippy`：当前环境未安装 clippy 组件，无法运行。开发机 / CI 须 `rustup component add clippy` 后纳入。
- [ ] 联网拉取 `workspace.dependencies` 的真实依赖编译：当前环境疑似离线，未验证 serde/tokio/axum/sqlx/reqwest 等的版本解析与 rustc 1.75 兼容性。**M0 首次引入外部依赖时需联网验证**，必要时下调版本或升级 toolchain。

## 需真实外部环境验证（按里程碑）

- [ ] M2：对真实 qBittorrent / qBittorrentEE 实例验证登录、拉 peer、封禁写入可见（含 EE shadowban）；封禁串与 Java 版逐字节对拍。
- [ ] M7：前端 `dist` 注入后端到端：登录、dashboard、实时日志 WS、下载器增删、手动封禁；状态码语义（401/403/303→/init）与前端一致；被删 PBH Plus 端点干净失效。
- [ ] M8：对真实 / 录制 BTN 端点验证 config 拉取、规则下行、心跳、上行 gzip 报文字段、PoW、游标续传。
- [ ] M6：GeoIP 对已知 IP（含中国省市/ISP）查询结果对拍。
- [ ] M10：单文件二进制 24h 连续运行无内存泄漏 / 无致命 SQLITE_BUSY / 到期解封正常。
