# 待测报告

> 只能人工 / 特定环境验证的项。

## 工具链 ✅ 已解决

- ✅ rustup `stable`(1.96) 已安装,含 clippy/rustfmt;`cargo clippy` 与 `cargo fmt --check` 已纳入,零告警。
- ✅ 外部依赖可联网拉取并在 1.96 编译通过(serde/tokio/sqlx 等)。注意：**系统 cargo 1.75 不可用**(现代依赖需 edition2024/≥1.85),必须用 `~/.cargo/bin/cargo`。

## 需真实外部环境验证（按里程碑）

- [ ] M2：对真实 qBittorrent / qBittorrentEE 实例验证登录、拉 peer、封禁写入可见（含 EE shadowban）；封禁串与上游逐字节对拍。
- [ ] M7：内置单页端到端：登录、状态、下载器增删、手动封禁、封禁历史、实时日志 WS、规则/订阅配置。
- [ ] M8：对真实 / 录制 BTN 端点验证 config 拉取、规则下行、心跳、上行 gzip 报文字段、PoW、游标续传。
- [ ] M6：GeoIP 对已知 IP（含中国省市/ISP）查询结果对拍。
- [ ] M9：单文件二进制 24h 连续运行无内存泄漏 / 无致命 SQLITE_BUSY / 到期解封正常。
