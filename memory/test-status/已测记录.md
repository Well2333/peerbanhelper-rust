# 已测记录

> 覆盖的代码自上次验证后未改动则不重复测试。

## 2026-06-19 — 骨架阶段

环境：rustc 1.75.0，离线（`CARGO_NET_OFFLINE=true`）。

- `cargo test --workspace`：**通过，18 个单元测试全绿**。
  - `pbh-domain`：6（PeerAction 优先级、CheckResult 合并、握手检测、PeerFlag 解析、IPv6 raw_ip）
  - `pbh-i18n`：4（`{}` 顺序填充、剩余占位保留、多余参数忽略、UTF-8 安全）
  - `pbh-rules`：4（StartsWith 大小写不敏感、False 短路、Length、空规则集）
  - `pbh-downloader`：1（工厂分发 qb/qbee/拒绝其它）
  - `pbh-engine`：1（恒等 NAT 透传）
  - `pbh-web`：2（StdResp ok/err）
- `cargo run -p pbh-server`：**通过**，打印启动横幅并干净退出。

覆盖的均为可隔离纯逻辑（守则第 8 条）。

## 2026-06-19 — 战略转向 v2（极简重构）

移除 `pbh-i18n`、`pbh-notify` 两 crate（弃用前端→无 i18n;砍推送/告警）。`pbh-web` 信封改为 `{ ok, data, error }`。

- `cargo test --workspace`：**通过,14 个单元测试全绿**（domain 6 / downloader 1 / engine 1 / rules 4 / web 2;i18n 的 4 个随 crate 移除）。
- `cargo run -p pbh-server`：**通过**,横幅指向 docs/05。

## 2026-06-19 — 文档整理（doc set 收敛为 4 份）

仅文档与 crate 文档注释/横幅字符串改动(无逻辑变更)。

- `cargo test --workspace`：**通过,无失败**(14 单测 + doc-tests)。
- 全仓 `grep` 校验:无对已删/改名文档(01-research-report/02-construction-guide/03-api-contract/04-db-schema/05-revised-strategy)的残留引用(changelog 历史除外)。

## 2026-06-19 — 删脚本引擎/AutoSTUN + 文档归位

删 `NatAddressProvider`/`IdentityNatProvider`(AutoSTUN)与脚本引擎抽象;docs/02 改名 02-roadmap,绑定性范围/架构归位 guidelines。

- `cargo test --workspace`：**通过,13 单测全绿**(domain 6 / downloader 1 / rules 4 / web 2;engine 的恒等 NAT 测试随抽象移除)。
- `cargo run -p pbh-server`：**通过**,横幅指向 memory/design/roadmap.md。
- `grep` 校验:无死链(旧文件名/已删 crate/已删抽象引用均空,changelog/source 除外)。

## 2026-06-19 — 架构设计文档移入 memory/design

把 architecture-analysis / roadmap / db-schema 由 docs/ 移到 memory/design/(docs/ 仅留守则)。

- `cargo test --workspace`：**通过,13 单测全绿**。
- `cargo run -p pbh-server`：横幅指向 `memory/design/roadmap.md`。
- `grep` 校验:全仓引用同步至新路径,无死链(test-status/changelog 历史叙述除外)。

## 2026-06-19 — M0 地基（toolchain=rustup stable 1.96）

环境：安装 rustup stable 1.96(系统 cargo 1.75 不可用);构建用 `~/.cargo/bin/cargo`。

- `cargo test --workspace`：**通过,24 单测全绿**(domain 8 / config 6 / storage 3 / rules 4 / downloader 1 / web 2)。
- `cargo clippy --workspace --all-targets`：**零告警**。`cargo fmt --check`：**干净**。
- `cargo run -p pbh-server`（PBH_DATA_DIR=临时）：
  - 首次：建 config/persist/logs 目录;写默认 config.yml/profile.yml;生成 token 写回;SQLite WAL 建库+迁移;生成安装ID;按日文件日志。
  - 二次：复用 token 与安装ID(持久化验证通过)。
- 覆盖纯逻辑(config 默认/解析、Paths、LogBuffer 环形、规则引擎等)+ storage 集成(内存库迁移/KV)。

## 2026-06-19 — M1 领域模型 + 规则引擎

- `cargo test --workspace`：**通过,37 单测全绿**(新增 matcher 8 / ip_matcher 3 / cache 2 / ban_list 4,替换原 rules 占位)。
- `cargo clippy`/`cargo fmt --check`：零告警/干净。
- 覆盖：匹配方法+FALSE 短路优先级、JSON RuleSet::parse、REGEX 大小写敏感、CIDR 最长前缀、moka pass-only 缓存、BanList ban/unban/到期清理/快照。均为可隔离纯逻辑。

## 2026-06-19 — M2 下载器（qB + qBEE）

- `cargo test --workspace`：**通过,45 单测全绿**（新增 pbh-downloader 9）。
- `cargo clippy`/`cargo fmt --check`：零告警/干净。
- 覆盖纯逻辑：全量封禁串(去重/换行/CIDR 门控/IPv6 压缩/单主机)、增量串(`|` 分隔)、DownloaderConfig YAML 往返、版本宽松解析、qB peer JSON 解析、工厂分发/拒绝。
- **HTTP 交互未验证**（无真实 qB），见待测报告 M2。

## 2026-06-19 — M3 流水线 + 调度 + BanManager（二进制可运行）

- `cargo test --workspace`：**通过,49 单测全绿**（新增 module 2 / manager 1 / repo 1 等）。
- `cargo clippy`/`cargo fmt --check`：零告警/干净。
- `./target/debug/pbh`（PBH_DATA_DIR=临时）实跑：启动→生成 token→建库→加载 0 下载器 + 3 默认模块→Ban Wave 启动→SIGINT 干净退出。日志/历史落库通路就绪。
- **真实 qB 封禁端到端未验证**（无实例）：登录/拉 peer/下发封禁待真机，见待测报告 M2。

## 2026-06-19 — M7 极简 Web + 内置单页（浏览器可测成品）

- `cargo test --workspace`：**通过,47 单测全绿**（pbh-web 无单测;旧 envelope 2 测随重写移除）。
- `cargo clippy`/`cargo fmt --check`：零告警/干净。
- **curl 端到端实测**（运行二进制后）：
  - `GET /` 返回内置 HTML 单页;
  - 无 token `GET /api/status` → 401 `{"ok":false,"error":"未授权"}`;
  - `POST /api/auth/login` 正确 token → `{"ok":true}`;
  - 带 Bearer `GET /api/status` → 正常 data;
  - `PUT /api/bans {ip:1.2.3.4}` → ok，`GET /api/bans` 列出该封禁（address 1.2.3.4/32）。
- 浏览器交互（添加真实 qB、看自动封禁）待用户用自己的 qB 验证。

## 2026-06-19 — 构建脚本 + 发布成品

- `build.sh`（release 配置去重 LTO）：`clippy` 子命令通过(-D warnings + fmt)。
- `./build.sh package`：release 构建 **1m19s** 成功 → 11M stripped 单文件 + `dist/pbh-0.1.0-linux-x86_64.tar.gz`(4.6M)。
- **release 二进制实跑验证**：`GET /` 出 HTML、登录、status 均正确;干净退出。
- 包内含 pbh + README + downloaders.yml.example + 运行说明.txt。

## 2026-06-19 — 真实 qBittorrent 联网验证（用户提供实例）

实例：`https://your-qb.example.com:50443`（qB **v5.1.3.10**，4 段版本→Enhanced Edition;libtorrent 1.2.20;有效 TLS 证书;admin/密码）。当时 0 个种子、`banned_IPs="198.51.100.10"`、`shadow_ban_enabled=false`。

用 release 成品实跑 + curl 旁证：
- ✅ **登录**（username/password → cookie SID，`/auth/login` 返回 "Ok."）。
- ✅ **版本/buildInfo** 拉取正常（解析 5.1.3.10→5.1.3，<5.3.0 故 range-ban 关，单 IP 封禁不受影响）。
- ✅ **get_torrents**（filter=active，0 个，无报错）。
- ✅ **apply_ban_list 全量模式**：经 Web API 手动封 `198.51.100.7`→下一轮 wave 下发→curl 旁证 qB `banned_IPs` 变为 `198.51.100.7`（**字节级写入确认**）。
- ✅ Web API 手动封禁链路（PUT /api/bans → wave → qB）。
- 测试后**已恢复** qB `banned_IPs` 至原值 `198.51.100.10`;`enable_multi_connections_from_same_ip` 全程未变（本就 false）。

**仍未在真机验证**（该 qB 无活动种子/peer）：`get_peers`、规则触发的自动封禁、增量封禁(`increment-ban=true`)、EE shadowban（服务端未启用）。需有活动种子+peer 时再验。

## 2026-06-19（续）— 真机 get_peers + 规则自动封禁验证

用户挂了 4 个活动种子（每个 5–14 leech）。peer 多为正常 qB/µTorrent 客户端（默认规则不命中，符合预期=不误封好客户端）。
为验证「拉 peer→规则→封禁→下发」全链路，临时配置 `peer-id-blacklist: STARTS_WITH "-UT"`（µTorrent）运行 release 成品 ~12s：
- ✅ **get_peers 解析正确**：正确解出 `client="μTorrent 3.6"`、`peer_id="-UT360W-/-UT355W-"`、`ip:port`。
- ✅ **规则匹配 + 自动封禁**：命中 6 个 µTorrent peer 并封禁（日志逐条 “封禁 110.67.160.145:16480 (μTorrent 3.6) — PeerID 命中黑名单: -UT360W-” …）。
- ✅ **下发到 qB 验证**：curl 旁证 qB `banned_IPs` 恰为这 6 个 IP（106.136.252.17 / 110.67.160.145 / 203.133.214.19 / 31.217.177.163 / 31.217.177.220 / 31.217.177.31）。
- 测试后**已恢复** qB `banned_IPs` 至原值 `198.51.100.10`。

**至此 M2 + M3 核心链路在真机全程跑通**：登录→拉 torrents→拉 peers→规则判定→自动封禁→写入 qB→历史落库。
仍待：增量封禁(`increment-ban=true` → `/transfer/banPeers`)、EE shadowban（需服务端开启）。
